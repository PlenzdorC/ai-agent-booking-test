# ========================================
# AgentBook Environment Variables
# ========================================
# 
# Instructions:
# 1. Copy this file and rename it to .env.local
# 2. Fill in your actual values (see instructions below)
# 3. NEVER commit .env.local to git (it's already in .gitignore)

# ========================================
# Application Settings
# ========================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=AgentBook

# ========================================
# Supabase Configuration (REQUIRED)
# ========================================
# How to get these:
# 1. Go to https://supabase.com and create a free account
# 2. Create a new project (takes ~2 minutes)
# 3. Go to Project Settings → API
# 4. Copy the values below:

# Project URL (looks like: https://xxxxxxxxxxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here

# anon/public key (starts with: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# service_role key (starts with: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...)
# ⚠️ KEEP THIS SECRET! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ========================================
# AI Agent Authentication
# ========================================
# Generate a random secret string (any random string is fine for development)
# You can use: openssl rand -base64 32
AGENT_API_SECRET=change_this_to_a_random_secret

# ========================================
# Email Notifications (OPTIONAL)
# ========================================
# Uncomment and fill these when you want to add email notifications
# Recommended services: Resend.com, SendGrid, AWS SES

# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=noreply@yourdomain.com

# ========================================
# Payment Processing (OPTIONAL)
# ========================================
# Add these later when you want to enable payments
# Get these from: https://dashboard.stripe.com/apikeys

# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ========================================
# Analytics (OPTIONAL)
# ========================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

